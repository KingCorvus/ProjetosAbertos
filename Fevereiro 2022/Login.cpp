#include "Login.hpp"

Login::Login(/* args */){
}

Login::~Login(){
}

bool Login::verificaLogin(Registr* head, std::string User, std::string password){
    if (head == nullptr) throw ExcessaoSemLogin();
    else{
        while (head != nullptr){
        }

    }
}
